<h1 style="text-align: center;text-decoration: underline;">BLOG</h1>

<form method="post" action="/insertData" style="text-align: center;" enctype="multipart/form-data">
    <input type="hidden" value="<%= single.id %>" name="editid">
    <table border="10" align="center" width="500" cellspacing="0" style="margin: auto; border-color: darkviolet;">
        <tr>
            <th style="padding: 15px;">Blog Title :</th>
            <td style="padding: 15px;"><input type="text" name="name" value="<%= single.name %>" style="width: 100%;"></td>
        </tr>
        <tr>
            <th style="padding: 15px;">Discription :</th>
            <td style="padding: 15px;"><input type="text" name="discription" value="<%= single.discription %>" style="width: 100%;"></td>
        </tr>
        <tr>
            <th style="padding: 15px;">Image Upload</th>
            <td style="padding: 15px;">
                <input type="file" name="image">
                <% if(single){ %>
                    <img src="<%= single.image %>" width="150">
                <% } %>
            </td>
        </tr>
        <tr>
            <th></th>
            <th>
                <% if(single) { %>
                    <input type="submit" value="Edit">
                <% } else{ %>
                    <input type="submit">
                <% } %>
            </th>
        </tr>
    </table>  
    <br><br><br>
    <table border="5" align="center" width="600" style="text-align: center;" cellspacing="0">
        <tr>
            <th style="padding: 15px;">Title</th>
            <th style="padding: 15px;">Discription</th>
            <th style="padding: 15px;">Image</th>
            <th style="padding: 15px;">Action</th>
        </tr>

        <% blogdata.map((val)=>{ 
            const {id,name,discription,image} = val
            %>
            <tr>
                <td><%= name %></td>
                <td><%= discription %></td>
                <td>
                    <img src="<%= image %>" width="100">
                </td>
                <td>
                    <a href="/deletedata?id=<%= id %>">Delete</a> ||
                    <a href="/editdata?id=<%= id %>">Edit</a>
                </td>
            </tr>
        <% }) %>
    </table>
</form>
